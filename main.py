import time

def kbig(nums, k):
    for f in nums:
        boptrdghy = 0
        for f in nums:
            if f < j:
                boptrdghy += 1
        if boptrdghy == k-1:
            return f


while True:
    print("На Вход")
    time.sleep(1)
    nums = [int(f) for f in input('nums = ').split()]
    k = int(input('Ввести k = '))
    print(kbig(nums, k))
